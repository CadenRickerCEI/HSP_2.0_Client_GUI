<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HSPGUI.Resources"
             x:Class="HSPGUI.Views.NetworkConfig">
    <!--<ContentView.Resources>
        <ResourceDictionary>
            <local:OctetValidationBehavior x:Key="octetValidation" />
        </ResourceDictionary>
    </ContentView.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Entry x:Name="octet1" WidthRequest="50" Placeholder="0" Keyboard="Numeric" MaxLength="3" Grid.Column="0">
            <Entry.Behaviors>
                <local:OctetValidationBehavior />
            </Entry.Behaviors>
        </Entry>
        <Label Text="." VerticalOptions="Center" Grid.Column="1" />
        <Entry x:Name="octet2" WidthRequest="50" Placeholder="0" Keyboard="Numeric" MaxLength="3" Grid.Column="2">
            <Entry.Behaviors>
                <local:OctetValidationBehavior />
            </Entry.Behaviors>
        </Entry>
        <Label Text="." VerticalOptions="Center" Grid.Column="3" />
        <Entry x:Name="octet3" WidthRequest="50" Placeholder="0" Keyboard="Numeric" MaxLength="3" Grid.Column="4">
            <Entry.Behaviors>
                <local:OctetValidationBehavior />
            </Entry.Behaviors>
        </Entry>
        <Label Text="." VerticalOptions="Center" Grid.Column="5" />
        <Entry x:Name="octet4" WidthRequest="50" Placeholder="0" Keyboard="Numeric" MaxLength="3" Grid.Column="6">
            <Entry.Behaviors>
                <local:OctetValidationBehavior />
            </Entry.Behaviors>
        </Entry>
    </Grid>-->   
</ContentView>
